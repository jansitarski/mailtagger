# Auto-activate this project's devenv shell on `cd` (direnv + nix-direnv).
# The `use devenv` helper is provided globally via the system's direnv stdlib.
# First time in this directory, run:  direnv allow
use devenv
